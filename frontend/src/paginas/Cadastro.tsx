import { useState, type FormEvent } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { cadastrar, guardarSessao } from '../servicos/autenticacao'
export function Cadastro() { const navegar = useNavigate(); const [erro, setErro] = useState<string | null>(null); async function enviar(evento: FormEvent<HTMLFormElement>) { evento.preventDefault(); const dados = new FormData(evento.currentTarget); try { guardarSessao(await cadastrar({ nomeMarcenaria: String(dados.get('nomeMarcenaria')), nomeResponsavel: String(dados.get('nomeResponsavel')), email: String(dados.get('email')), senha: String(dados.get('senha')) })); navegar('/orcamentos') } catch (causa) { setErro(causa instanceof Error ? causa.message : 'Não foi possível criar a conta.') } } return <main className="cartao"><h1>Crie a conta da sua marcenaria</h1><form onSubmit={enviar}><label>Nome da marcenaria<input name="nomeMarcenaria" required /></label><label>Seu nome<input name="nomeResponsavel" required /></label><label>E-mail<input name="email" type="email" required /></label><label>Senha<input name="senha" type="password" minLength={8} required /></label>{erro && <p role="alert">{erro}</p>}<button>Criar conta</button></form><p>Já tem conta? <Link to="/entrar">Entrar</Link></p></main> }
