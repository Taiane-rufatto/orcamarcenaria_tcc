# Evidências — incremento 000

**Data:** 2026-09-18

| Verificação | Evidência observada |
|---|---|
| API e banco | `GET http://localhost:3333/saude` respondeu `{"api":"ok","banco":"ok",...}`. |
| Frontend | Servidor Vite respondeu HTTP 200 em `http://127.0.0.1:5173/`; a tela usa `consultarSaude()` para exibir o estado da API e do banco. |
| Testes do backend | `npm test` no diretório `backend/`: 1 arquivo e 2 testes aprovados. |
| Compilação do backend | `npm run build` no diretório `backend`: concluído com sucesso após configurar a resolução TypeScript `node16`. |
| Compilação do frontend | `npm run build` no diretório `frontend`: concluído com sucesso. |
| Repositório remoto | `origin/main` confirmou o commit `87dde91` no GitHub. |
| Segredos e dependências | `git check-ignore` confirmou que `backend/.env`, `frontend/.env` e ambos os diretórios `node_modules` estão ignorados. |

Não houve alteração de fórmula, valor monetário, quantidade, percentual ou arredondamento neste incremento; portanto, a rotina dos dez casos de cálculo não se aplica.
